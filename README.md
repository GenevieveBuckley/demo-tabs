# Minimal tabs example

A working example of switching content based on tab selection.

## Setup

```
git clone https://github.com/GenevieveBuckley/demo-tabs.git
cd demo-tabs
npm install ci
```

## Run

```
npm run dev
```

Then open a browser to the localhost address.
